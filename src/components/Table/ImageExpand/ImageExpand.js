import React from 'react'
import {ImageContainer} from './ImageExpandElements'

const ImageExpand = ({imageOpen}) => {
  return (
    <ImageContainer imageOpen={imageOpen}>Image Expand</ImageContainer>
  )
}

export default ImageExpand